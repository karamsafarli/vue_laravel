<template>

<section class="our-mission">
    <h1 class="heading">Our Mission</h1>
    <p class="content">To provide access to world-class financial products to every Indian with a Smartphone</p>
</section>
<section class="section-2">
    <div class="key-principles">
        <h1 class="heading">We operate on 3 key principles</h1>
        <div class="content">
            <div class="principle"><img src="https://moneyview.in/images/personalized.png" alt="Fully Personalized">
                <div>
                    <h2 class="principle-heading">Fully Personalised</h2>
                    <p class="principle-text">Credit and loan solution that is best suited for the user’s needs and financial profile</p>
                </div>
            </div>
            <div class="principle"><img src="https://moneyview.in/images/full_digital_process.png" alt="Frictionless Digital Process">
                <div>
                    <h2 class="principle-heading">Frictionless Digital Process</h2>
                    <p class="principle-text">Leveraging technology to offer a simple &amp; intuitive user-experience</p>
                </div>
            </div>
            <div class="principle"><img src="https://moneyview.in/images/lending.png" alt="Responsible Lending">
                <div>
                    <h2 class="principle-heading">Responsible Lending</h2>
                    <p class="principle-text">Graduation-based strategy opens new offers for customers as they build a healthy credit history. We work with 100% transparency with ZERO surprises!</p>
                </div>
            </div>
        </div>
    </div>
    <div class="content">
        <p class="mv-why-us text-left">In a country of more than a billion people, only 10% are eligible for formal credit products. Our vision is to provide world-class financial products &amp; services to those 90% of the underserved Indians.</p>
        <p class="mv-why-us text-left">We’re making this possible through our AI powered proprietary models that predict risk more accurately than traditional models used in the industry, thus ensuring right credit solutions are provided to the right users in a simplified manner!</p>
    </div>
</section>
<section class="section-2 flex-column">
    <h2 class="sectionHeading">We’re committed to excellence!</h2>
    <p class="sectionContent">moneyview is amongst very few Indian Fintech companies to be <strong>ISO 27001:2022</strong> Certified for Information Security Management Systems.</p>
</section>
<section class="about-us-section">
    <p class="about-us-section-paragraph">We want to make a meaningful<br>contribution towards driving true<br>financial inclusion in India!</p>
    <div class="about-us-section-display">
        <div class="display-flex section-padding">
            <div class="about-us-section-image "><img src="https://dugtmg0pklp2w.cloudfront.net/download-icon.svg" class="about-us-section-image-container"></div>
            <div class="about-us-section-container">
                <p class="about-us-section-title">45 M+</p>
                <p class="about-us-section-content">App Downloads</p>
            </div>
        </div>
        <div class="display-flex section-padding">
            <div class="about-us-section-image"><img src="https://dugtmg0pklp2w.cloudfront.net/wallet-icon.svg" class="about-us-section-image-container"></div>
            <div class="about-us-section-container">
                <p class="about-us-section-title">12,000 Cr+</p>
                <p class="about-us-section-content">Loan Disbursed</p>
            </div>
        </div>
        <div class="display-flex section-padding">
            <div class="about-us-section-image"><img src="https://dugtmg0pklp2w.cloudfront.net/maps-icon.svg" class="about-us-section-image-container"></div>
            <div class="about-us-section-container">
                <p class="about-us-section-title">19,000+</p>
                <p class="about-us-section-content margin0">Locations Served</p>
            </div>
        </div>
    </div>
</section>
<section class="investor-section">
    <p class="section-heading">Our Investors</p>
    <div class="investor-class">
        <div class="investors-container"><img src="https://moneyview.in/images/ic-TigerGlobal.svg" class="investors-logo"></div>
        <div class="investors-container"><img src="https://moneyview.in/images/ic-Accel.svg" class="investors-logo"></div>
        <div class="investors-container"><img src="https://moneyview.in/images/ic-RibbitCapital.svg" class="investors-logo"></div>
        <div class="investors-container"><img src="https://moneyview.in/images/ic-WinterCapital.svg" class="investors-logo"></div>
        <div class="investors-container"><img src="https://moneyview.in/images/evolvence_india.png" class="investors-logo"></div>
    </div>
</section>
<section class="founders-section">
    <p class="section-heading">Our Founders</p>
    <div class="founders-section-display">
        <div class="founders-border-box">
            <div class="founders-details"><img src="https://moneyview.in/images/puneeth_aggarwal.png" alt="avatar" class="founders-image">
                <div class="founders-class">
                    <p class="founders-name">Puneet Agarwal</p>
                    <p class="founders-title">CEO, moneyview</p>
                </div>
            </div>
            <p class="founders-content">An IIT alumnus, Puneet started his career at Mckinsey. From there he moved to Capital One in the US, where he was directly involved in various new product development &amp; payment solutions. He's also worked with Google and was instrumental in driving the Gpay in its early years in the US. In 2014 he co-founded moneyview with a vision to simplify access to credit for all Indians.</p>
        </div>
        <div class="founders-border-box box-shadow-color">
            <div class="founders-details"><img src="https://moneyview.in/images/sanjay_aggarwal.png" alt="avatar" class="founders-image">
                <div class="founders-class">
                    <p class="founders-name">Sanjay Aggarwal</p>
                    <p class="founders-title">CTO, moneyview</p>
                </div>
            </div>
            <p class="founders-content">Sanjay comes with over two decades of experience in technology. He has worked with various IT companies, including Infosys, and helped scale their engineering &amp; technology. He was working at Yahoo before starting his first venture, Mingle box, an ed-tech platform, and later on co-founded moneyview along with Puneet. With a passion for technology, Sanjay has built infra for various web server layers, cloud systems, and network storage for moneyview.</p>
        </div>
    </div>
</section>

<div class="modal fade popupModal" id="popupModal" role="dialog" aria-labelledby="popupModal" aria-hidden="true" tabindex="-1" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog popupModalDialog modal-dialog-centered" role="document">
        <div class="modal-content" style="border-radius: 24px;">
            <div class="campaignPopup">
                <a class="CT_popup_banner_dec23 web-ct-popup" href="https://moneyview.in/credit-score/ctl/login?utm_source=website_CT_popup_ctl&amp;utm_medium=/about-us&amp;utm_campaign=brand-page">
                    <img class="popupBanner displayDesktop" src="https://moneyview.in/images/ic-ipl-popup-banner-desktop.jpg" alt="modal_image">
                    <img class="popupBanner displayMobile" src="https://moneyview.in/images/ic-ipl-popup-banner-mobile.jpg" alt="modal_image">
                </a>
                <button id="popupCloseButton" type="button" class="close closeButton closePopup" data-dismiss="modal" aria-label="Close"> <span aria-hidden="true">×</span> </button>
            </div>
        </div>
    </div>
</div>

<!-- <div class="container-fluid bottom-sheet hideStickyFooterWithClass" style="display: flex;">
    <div class="lead_gen_form mainContainer bottom_sheet_grid_container marginTop0px PLloginForm" id="loginForm" style="display: grid;">
        <div class="footerURL display_none_non_important">brand_page_leads,brand_page_leads_footer_stickybar_about-us</div>

        <div class="bottom_sheet_heading_container apply-for-personal-loan-text-grid " style="display: grid">
            <p class="bottom-sheet-text undefined">Apply for personal loan</p>
        </div>

        <div class="lead_gen_container bottom_sheet_lead_gen_grid  newcustomGridStyles" width="80%">
            <div class="generate_otp_container gridContainer" id="generate_otp_wrap">
                <div class="bottom-most-banner-input-container">
                    <span class="country_code">+91</span>
                    <input type="number" id="bottom_sheet_input" class="bottom-most-banner-input bottom_sheet_input input_style generate_otp_input" placeholder="Mobile no.">
                </div>
                <div class="apply-loan-btn-container">
                    <button class="apply-loan-btn apply_now_lead_gen_btn bottom-sheet-btn-style seo_page_pl_footerstickybar_applynow_cta bottom_sheet_ct_btn ">
                        <span class="btn_loader spinner-border spinner-border-sm display_none_non_important" role="status" aria-hidden="true"></span>
                        <p class="bottom-sheet-btn-text-style sticky-footer-btn-text">Apply now</p>
                    </button>
                </div>
                <p id="otp_message" class="footerStickyOtpMessage generate_otp_message message_container color-white" style="display: none"></p>
                <div class="bottom-most-banner-consent-container">
                    <div id="bottom_sheet_consent" class="bottom-most-banner-consent-wrap consent_checkbox_wrap loginConsent">
                        <input type="checkbox" class="form-check-input generate_otp_consent" id="bottom_sheet_consent_checkbox" checked="">
                        <label class="consent bottom-most-banner-consent-text align-items-center loginConsentText" for="bottom_sheet_consent_checkbox">By proceeding, you agree to our&nbsp; <a target="”_blank”" href="/terms-conditions-loans-app">Terms &amp; Conditions</a> &nbsp;&amp;&nbsp;<a target="”_blank”" href="/privacy-policy-loans">Privacy Policy</a>
                        </label>
                    </div>
                </div>
            </div>
            <div class="bottom_sheet_verify_otp_container verify_otp_container bottom_sheet_lead_gen_grid display_none_non_important">
                <div class="gridContainer">
                    <div class="bottom-most-banner-input-container">
                        <input type="number" id="bottom_sheet_verify_otp_lead_gen_input" class="bottom_sheet_verify_otp_lead_gen_input verify_otp_input input_style" placeholder="Enter OTP" maxlength="5">
                    </div>
                    <div class="apply-loan-btn-container">
                        <button type="button" class="bottom-sheet-btn-style bottom_sheet_verify_otp_lead_gen_btn verify_otp_lead_gen_btn seo_page_pl_footerstickybar_verify_otp_cta">
                            <p class="bottom-sheet-btn-text-style">Verify OTP</p>
                        </button>
                    </div>
                    <p class="otp_error_message message_container" style="display: none;"></p>
                </div>
                <div class="verify_otp_message_container">
                    <div class="verify_otp_mobile_number_container">
                        <span class="edit_mobile_number_text"></span>
                        <button class="edit_mobile_number text-button-link">Edit</button>
                    </div>
                    <button id="resend_otp" type="button" class="text-button-link resend_otp">Resend OTP</button>
                </div>
            </div>
        </div>

        <div class="verified_bottom_sheet fullRowClass display_none_non_important">
            <div class="verified_container">
                <img src="https://moneyview.in/images/ic-greenTickComplete.svg" width="48px" height="48px" class="marginBottom16" alt="Green Tick Success" loading="lazy">

                <h4 class="verified_bottom_sheet_text marginBottom8">Thank you for your interest!</h4>

                <p class="verified_bottom_sheet_desc marginBottom24">We will reach out to you shortly</p>

                <button class="verified_success_btn verifyBtnCustom"> Okay </button>
            </div>
        </div>
    </div>
    <div class="mainContainer continuePLApplication displayNone marginTop0" style="display: none;">
        <div class="fullRowClass">
            <button class="continueAppButton stickyButtonFont PLButton seo_page_pl_continue_loan_application_footer_cta">Continue loan application</button>
        </div>
    </div>
</div> -->



<!-- <script>
    window.addEventListener('load', function (e) {
        let leadGenSectionObserver = new IntersectionObserver(function (entries) {
            const navbar = document.querySelector('.navbar-custom'),
                pageCategory = 'Brand Page';
            const navbarColor = '{"Personal Loan Page" : "linear-gradient(109deg, #F0F8F3 37.69%, #FFF 86.94%)", "ProSaver Page": "linear-gradient(109deg, #E8E1EE 37.69%, #FFF 86.94%)", "Credit Score Product Page": "linear-gradient(109deg, rgba(255, 236, 228, 0.89) 37.69%, #FFF 86.94%)", "Business Loan Page" : "linear-gradient(269deg, #D9E2DF 42.58%, #D9E2DF 43.45%)", "Smart Pay Page": "linear-gradient(109.47deg, rgba(20, 72, 53, 0.16) 37.69%, rgba(20, 72, 53, 0.08) 86.94%), #FFFFFF"}';

            let bgColor = JSON.parse(navbarColor)[pageCategory];
            if (entries[0].isIntersecting === true) {
                navbar.style.background = bgColor;

            } else {
                navbar.style.background = "white";
            }
        }, {
            threshold: [0]
        });

        document.querySelector(".firstSection") && leadGenSectionObserver.observe(document.querySelector(".firstSection"));

        let [apiUrl, backendUrl, pwaUrl, creditUrl] = 'https://cms-api.moneyview.in/v1/api/,https://moneyview.whizdm.com/loans/services/api/web/,https://moneyview.in/apply-loan/,https://moneyview.in/credit-score/'.split(","), pageCategory = 'Brand Page', pathname = '/about-us';
        let ctrlDown = false,
            apiEndPoint = backendUrl,
            redirectUrl = pageCategory === 'Credit Score Page' || pageCategory === 'CT Campaign Page' || pageCategory === 'Credit Score Product Page' ? creditUrl : pwaUrl,
            otpId = "",
            timeOut, intervalId, isHidePopup = 'true';

        function disableButtonHandler(parentContainer) {
            const isConsentCheckbox = parentContainer.querySelector(".generate_otp_consent");
            const applyNowLeadGenBtn = parentContainer.querySelector('.apply_now_lead_gen_btn');
            const verifyOtpLeadBtn = parentContainer.querySelector(".verify_otp_lead_gen_btn");

            if (isConsentCheckbox !== null && isConsentCheckbox.checked) {
                applyNowLeadGenBtn && applyNowLeadGenBtn.removeAttribute('disabled');
                verifyOtpLeadBtn && verifyOtpLeadBtn.removeAttribute('disabled');
            } else {
                applyNowLeadGenBtn && applyNowLeadGenBtn.setAttribute('disabled', true);
                verifyOtpLeadBtn && verifyOtpLeadBtn.setAttribute('disabled', true);
            }
        }

        function setMobileInputError(parentContainer, errorMessage) {
            const generateOtpMessage = parentContainer.querySelector(".generate_otp_message");
            generateOtpMessage.style.display = "grid";
            generateOtpMessage.innerText = errorMessage;
            generateOtpMessage.style.color = "#D6273C";
            generateOtpMessage.classList.add('input_error_text');
            parentContainer.querySelector(".generate_otp_input").classList.add('error_border');
        }

        function validateMobileNumber(value, parentContainer) {
            const mobileReg = /^[6789]\d{9}$/;

            if (value.length === 0) {
                setMobileInputError(parentContainer, 'Please enter mobile number');
                return false;
            } else if (value.length < 10 || !mobileReg.test(value)) {
                setMobileInputError(parentContainer, 'Please enter valid mobile number');
                return false;
            } else {
                const generateOtpMessage = parentContainer.querySelector(".generate_otp_message");
                const generateOtpInput = parentContainer.querySelector(".generate_otp_input");
                generateOtpMessage.textContent = "";
                generateOtpMessage.style.color = "";
                generateOtpMessage.classList.remove('input_error_text');
                generateOtpInput.classList.remove('error_border');
                if (!parentContainer.querySelector(".footerStickyOtpMessage")) {
                    generateOtpMessage.textContent = 'An OTP will be sent for verification';
                }
                return true;
            }
        }

        function handleMobileInputChange(event) {
            const id = event.target.id;
            let value = event.target.value;

            // Mobile number input handling
            if (value.length === 10 && event.keyCode > 47 && event.keyCode < 58) {
                event.preventDefault();
            }

            // cap number of digits to 10
            if (value.length > 10) {
                value = value.slice(0, 10);
                event.target.value = value;
            }
            const parentContainer = event.target.closest(".lead_gen_form");
            const generateOtpMessage = parentContainer.querySelector(".generate_otp_message");
            const generateOtpInput = parentContainer.querySelector(".generate_otp_input");
            generateOtpMessage.textContent = "";
            generateOtpMessage.style.color = "black";
            if (id === "bottom_sheet_input")
                generateOtpMessage.style.display = "none";
            if (!parentContainer.querySelector(".footerStickyOtpMessage")) {
                generateOtpMessage.textContent = 'An OTP will be sent for verification';
            }
            generateOtpInput.classList.remove('error_border');
        }

        let verifyOtpInput = document.querySelectorAll('.verify_otp_input');
        let generateOTP = document.querySelectorAll('.generate_otp_input');
        window.addEventListener("pageshow", function () {
            verifyOtpInput.forEach(element => {
                element.value = "";
            });
            generateOTP.forEach(element => {
                element.value = "";
            });
        });

        generateOTP.forEach(function (element) {
            element.addEventListener("input", function (event) {
                handleMobileInputChange(event);
            })
        });

        document.querySelectorAll("input[type='number']").forEach(function (element) {
            let keyBoardKUnicodeMap = {
                controlKeys: {
                    ctrl: 17,
                    windows: 91,
                },
                otherKeys: {
                    keya: 65,
                    keyc: 67,
                    keyv: 86,
                    keyx: 88,
                },
                backSpace: 8,
            };
            element.addEventListener("keydown", event => {
                if (Object.values(keyBoardKUnicodeMap.controlKeys).includes(event.keyCode)) ctrlDown = true;
                let value = event.target.value;
                const maxLength = parseInt(element.getAttribute('maxlength'), 10);
                if ((maxLength && value.length >= maxLength) && !(event.keyCode === keyBoardKUnicodeMap.backSpace) && !(ctrlDown && Object.values(keyBoardKUnicodeMap.otherKeys).includes(event.keyCode))) event.preventDefault();
                const PHONENUMBER_ALLOWED_CHARS_REGEXP = /[0-9\/]+/;
                if (!(PHONENUMBER_ALLOWED_CHARS_REGEXP.test(event.key) || event.keyCode === keyBoardKUnicodeMap.backSpace || (ctrlDown && Object.values(keyBoardKUnicodeMap.otherKeys).includes(event.keyCode)))) {
                    event.preventDefault();
                }
            })
            element.addEventListener("keyup", event => {
                if (Object.values(keyBoardKUnicodeMap.controlKeys).includes(event.keyCode)) ctrlDown = false;
            })
        });

        verifyOtpInput.forEach(function (element) {
            element.addEventListener("input", function (event) {
                handleOtpChange(event);
            })
        });

        document.querySelectorAll('.apply_now_lead_gen_btn').forEach(function (element) {
            element.addEventListener("click", function (event) {
                sendOtp(false, event);
            })
        });

        document.querySelectorAll('.resend_otp').forEach(function (element) {
            element.addEventListener("click", function (event) {
                sendOtp(true, event);
            })
        });

        document.querySelectorAll('.edit_mobile_number').forEach(function (element) {
            element.addEventListener("click", function () {
                const parentContainer = element.closest(".lead_gen_form");
                edit(parentContainer);
            })
        });

        document.querySelectorAll('.verify_otp_lead_gen_btn').forEach(function (element) {
            element.addEventListener("click", function () {
                const parentContainer = element.closest(".verify_otp_container");
                let parentElement = parentContainer.querySelector('.verify_otp_lead_gen_btn')
                parentElement.setAttribute("disabled", true);
                let spanElement = createDomElement("span", {
                    "class": "btn_loader spinner-border spinner-border-sm ctButtonLoader",
                    "role": 'status',
                    "aria-hidden": 'true',
                    "style": "display: inline-block",
                });
                let textElement = createDomElement("p", {
                    "class": "bottom-sheet-btn-text-style ctButtonClass",
                }, ``);
                while (parentElement.firstChild) parentElement.removeChild(parentElement.firstChild);
                parentElement.appendChild(spanElement);
                parentElement.appendChild(textElement);

                validateOtp(parentContainer);
            })
        });

        document.querySelectorAll('.generate_otp_consent').forEach(function (element) {
            element.addEventListener("change", function (event) {
                const parentContainer = element.closest(".lead_gen_container");
                disableButtonHandler(parentContainer);
            })
        });

        function sendOtp(flag, event) {
            let parentContainer = event.target.closest('.lead_gen_form');
            if (validateMobileNumber(parentContainer.querySelector('.generate_otp_input').value, parentContainer)) {
                parentContainer.querySelector('.apply_now_lead_gen_btn').setAttribute("disabled", true);
                if (parentContainer.querySelector('.apply_now_lead_gen_btn .btn_loader')) parentContainer.querySelector('.apply_now_lead_gen_btn .btn_loader').style.display = 'inline-flex';
                if (parentContainer.querySelector('.apply_now_lead_gen_btn .bottom-sheet-btn-text-style'))
                    parentContainer.querySelector('.apply_now_lead_gen_btn .bottom-sheet-btn-text-style').style.display = 'none';
                generateOtp(flag, parentContainer);
            }
        }

        function generateOtp(resendFlag, parentContainer) {
            let mobile = parentContainer.querySelector(".generate_otp_input").value;
            let sectionContainer = parentContainer.closest(".lead_gen_form");
            let timerElement = document.querySelector("#timer"),
                duration = 20,
                resendButton = document.getElementById("resend_otp");

            let request = new XMLHttpRequest();
            request.open("POST", apiEndPoint + "otp/generate");
            request.onload = function () {
                if (request.status === 200) {
                    let res = JSON.parse(request.response);
                    if (res.status === "SUCCESS") {
                        sessionStorage.setItem("exitPopupShown", true);
                        if (parentContainer.classList.contains("popupLoginContainer")) {
                            $('#popupModal').modal("hide");
                            parentContainer = document.querySelector(".loginSection");
                            parentContainer.querySelector('.generate_otp_input').value = mobile;
                        }
                        otpId = res.otpId;
                        parentContainer.querySelector('.apply_now_lead_gen_btn').removeAttribute("disabled");

                        if (parentContainer.querySelector('.apply_now_lead_gen_btn .btn_loader')) parentContainer.querySelector('.apply_now_lead_gen_btn .btn_loader').style.display = 'none';

                        if (parentContainer.querySelector('.apply_now_lead_gen_btn .bottom-sheet-btn-text-style'))
                            parentContainer.querySelector('.apply_now_lead_gen_btn .bottom-sheet-btn-text-style').style.display = 'inline-flex';

                        parentContainer.closest('.lead_gen_form').querySelector(".generate_otp_container").style.display = "none";

                        parentContainer.closest('.lead_gen_form').querySelector(".consent_checkbox_wrap").style.display = "none";

                        parentContainer.closest('.lead_gen_form').querySelector(".verify_otp_container").style.display = "flex";

                        parentContainer.closest('.lead_gen_form').querySelector('.verify_otp_input').focus();

                        parentContainer.closest('.lead_gen_form').querySelector(".edit_mobile_number_text").textContent = `+91-${mobile}`;

                        parentContainer.closest('.lead_gen_form').querySelector(".otp_error_message").style.display = "none";

                        parentContainer.closest('.lead_gen_form').querySelectorAll(".verify_otp_input").forEach(element => {
                            element.value = "";
                        });

                        if (pageCategory === 'CT Campaign Page') {
                            timerElement.innerHTML = '';
                            document.querySelector('#resend_otp').style.display = "none";
                            document.querySelector('#otp-timer').style.display = 'inline-block';
                            startTimer(duration, timerElement, resendButton);
                        } else {
                            setResendTime();
                        }

                        if (resendFlag) {
                            parentContainer.closest('.lead_gen_form').querySelectorAll(".verify_otp_input").forEach(element => {
                                element.value = "";
                            });
                            parentContainer.querySelector('.otp_error_message').classList.remove('display_block');
                            parentContainer.querySelector('.otp_error_message').classList.remove('input_error_text');
                            parentContainer.querySelectorAll(".verify_otp_input").forEach(element => {
                                element.classList.remove('error_border');
                            });
                            $("#alert").html("OTP sent successfully to your number").show();
                        }
                    } else {
                        alert("Something went wrong! Please try again.");
                    }
                }
            };

            const generateOtpFormData = new FormData();
            let source = pageCategory === 'Credit Score Product Page' || pageCategory === 'Credit Score Page' || pageCategory === 'CT Campaign Page' ? "CREDIT-VIEW-WEB" : pageCategory === 'ProSaver Page' || pathname === "/pro-saver-banking-faq" || pathname == "/banking-faq" ? "prosaver" : "pwa";

            generateOtpFormData.append("key", "MOBILE");
            generateOtpFormData.append("mobile", mobile);
            generateOtpFormData.append("source", source);

            request.send(generateOtpFormData);
        }

        function setOtpInputError(parentContainer, errorMessage) {
            parentContainer.querySelectorAll(".verify_otp_input").forEach(element => {
                element.classList.add("error_border");
            });

            if (errorMessage.length > 30 && parentContainer.parentNode.classList.contains("isModal")) {
                parentContainer.querySelector('.verify_otp_message_container').classList.add('flex-column');
                parentContainer.querySelector('.alignItemsEnd').style.justifyContent = "unset";
            }

            parentContainer.querySelector('.otp_error_message').innerHTML = errorMessage;
            parentContainer.querySelector('.otp_error_message').classList.add('display_block');
            parentContainer.querySelector('.otp_error_message').classList.add('input_error_text');
        }

        let utmSource, utmCampaign, utmMedium, ad, adgroup;

        function utmParameters(parentContainer) {
            if (pageCategory === 'CT Campaign Page') {
                if (window.location.search === null || window.location.search === "") {
                    utmSource = "credit_tracker";
                    utmMedium = "organic_channel";
                    utmCampaign = null;
                }
            } else if ((pageCategory === 'Personal Loan Page' || pageCategory === 'Credit Score Product Page' || pageCategory === 'Business Loan Page') && parentContainer.classList.contains("header_verify_otp")) {
                let category = pageCategory.replace(/ /g, "_").toLowerCase() + "_leads";
                utmSource = category;
                utmCampaign = `${category}_main_signup`;
                utmMedium = pageCategory === 'Personal Loan Page' ? "pl_product_lps" : "rganic_channel";
            } else if (parentContainer.classList.contains("bottom_sheet_lead_gen_grid")) {
                [utmSource, utmCampaign] = document.querySelector('.footerURL').textContent.split(",");
                utmMedium = "organic_channel";
            }
        }

        function showValidatedSuccessBottomSheet(parentContainer) {

            parentContainer.closest('.lead_gen_form').querySelector(".bottom_sheet_heading_container").style.display = "none";

            parentContainer.closest('.lead_gen_form').querySelector(".verify_otp_container").style.display = "none";

            parentContainer.closest('.lead_gen_form').querySelector(".verified_bottom_sheet").style.display = "grid";

            // on "okay" button click
            const bottomSheet = document.querySelector(".bottom-sheet");
            const button = document.querySelector(".verified_success_btn");

            button.addEventListener("click", function (event) {
                bottomSheet.style.display = "none";

                bottomSheet.style.setProperty("display", "none", "important");
                // removing this class, as we don't want bottom sheet to open on scroll.
                bottomSheet.classList.remove("hideStickyFooterWithClass");
                bottomSheet.classList.remove("onScrollFooterFixed");
            });
        }

        function isDesktop() {
            return window.matchMedia("(min-width: 768px)").matches;
        }

        function updateButtonText() {
            if (pageCategory === "Credit Score Product Page" || pageCategory === "Credit Score Page") {
                let paragraph = document ? .querySelector(".sticky-footer-btn-text");
                let isDesktopUser = isDesktop();
                paragraph.innerText = isDesktopUser && paragraph ? 'Get score & more' : 'Get score';
            }
        }
        //initialize
        updateButtonText();
        // on resize
        window.addEventListener("resize", updateButtonText);

        function validateOtp(parentContainer) {
            let otp = '';
            let request = new XMLHttpRequest();
            let paragraphElement = createDomElement("p", {
                "class": "bottom-sheet-btn-text-style ctButtonClass",
            }, 'Verify OTP');
            let parentElement = parentContainer.querySelector('.verify_otp_lead_gen_btn');
            let checkedValue = document.querySelector("#whatsAppCheckBox") && document.querySelector("#whatsAppCheckBox").checked;
            parentContainer.querySelectorAll('.verify_otp_input').forEach(element => {
                otp += element.value;
            })
            if (otp.length === 0) {
                setOtpInputError(parentContainer, 'Please enter OTP');
                parentElement.removeAttribute("disabled");
                while (parentElement.firstChild) parentElement.removeChild(parentElement.firstChild);
                parentElement.appendChild(paragraphElement);
            } else if (otp.length < 5) {
                setOtpInputError(parentContainer, 'Please enter valid OTP');
                parentElement.removeAttribute("disabled");
                while (parentElement.firstChild) parentElement.removeChild(parentElement.firstChild);
                parentElement.appendChild(paragraphElement);
            } else if (otp.length === 5) {
                // get user device
                const userPlatforms = {
                    Windows: "Windows",
                    Macintosh: "MacOS",
                    Android: "Android",
                    Linux: "Linux",
                    iPhone: "iOS",
                    iPad: "iOS",
                    iPod: "iOS",
                    default: "Other",
                };

                utmParameters(parentContainer);
                let queryString;
                if (window.location.search) {
                    queryString = new URLSearchParams(window.location.search.substring(1));
                } else {
                    const query = (pageCategory === 'CT Campaign Page') ? "" : "otp_widget=1&";
                    queryString = query + "utm_source=" + utmSource + "&utm_medium=" + utmMedium + "&utm_campaign=" + utmCampaign;
                }
                let params = new URLSearchParams(queryString);

                const queryParams = {};

                params.forEach((value, key) => {
                    queryParams[key] = value;
                });

                let attributiondata = JSON.stringify(queryParams);

                const userAgent = navigator.userAgent;
                const platformKey = Object.keys(userPlatforms).find((platform) => userAgent.includes(platform));
                let validateOtpApi = pageCategory === 'Credit Score Page' || pageCategory === 'CT Campaign Page' || pageCategory === 'Credit Score Product Page' ? "creditView/verify-otp" : pageCategory === 'ProSaver Page' || pathname === "/pro-saver-banking-faq" || pathname == "/banking-faq" ? "otp/verify/mv-lead" : "otp/verify/V2";
                request.open("POST", apiEndPoint + validateOtpApi);
                // set user-platform header
                request.setRequestHeader("user-platform", userPlatforms ? . [platformKey] ? ? userPlatforms.default);
                request.setRequestHeader("Attributiondata", attributiondata);
                request.onload = function () {
                    if (request.status === 200) {
                        let res = JSON.parse(request.response);
                        if (res.status === "SUCCESS") {
                            if (pageCategory === 'ProSaver Page' || pathname === "/pro-saver-banking-faq" || pathname == "/banking-faq") {
                                return showValidatedSuccessBottomSheet(parentContainer);
                            }
                            window.open(
                                redirectUrl + "utility/autologin?" + queryString + "&autologin_token=" + res.token, "_self"
                            );
                            resetDialog(parentContainer);
                        } else {
                            parentElement.removeAttribute("disabled");
                            while (parentElement.firstChild) parentElement.removeChild(parentElement.firstChild);
                            parentElement.appendChild(paragraphElement);

                            let errorMessage = 'Something went wrong';
                            if (res.error && res.error.errorMessage) {
                                setOtpInputError(parentContainer, res.error.errorMessage);
                            } else {
                                parentContainer.querySelector('.otp_error_message').textContent = "";
                                parentContainer.querySelector('.otp_error_message').classList.remove('display_block');
                                parentContainer.querySelector('.otp_error_message').classList.remove('input_error_text');
                                parentContainer.querySelectorAll(".verify_otp_input").forEach(element => {
                                    element.classList.remove('error_border');
                                });

                                if (res.showAppDownload) {
                                    if (res ? .atlasPartnerDownloadInformation ? .partnerName.toLowerCase() === "flipkart") {
                                        errorMessage = `Please continue the loan application in <a href="${res.atlasPartnerDownloadInformation.playStoreUrl}" target="_blank" class="errorLinks"> Flipkart app</a>.`;
                                    } else {
                                        errorMessage = 'Multiple accounts linked to mobile. Please <a href="https://mvl.onelink.me/504844896/duli15yc" target="_blank" class="errorLinks">download the app</a> to continue.';
                                    }
                                    setOtpInputError(parentContainer, errorMessage);
                                } else if (res.emailCheckRequired) {
                                    errorMessage = 'Email Verification Required. Please go to this <a href="https://moneyview.in/apply-loan" class="alert-link" target="_blank">link</a> to verify your Mobile and Email';

                                    setOtpInputError(parentContainer, errorMessage);
                                }
                            }
                        }
                    }
                };

                let consentContainer = parentContainer.parentNode
                let whatsAppConsentValue = checkedValue ? "GIVEN" : "NOT_GIVEN",
                    whatsAppConsentObject, loginConsentObject;
                let whatsAppConsent = consentContainer.querySelector(".whatsAppConsent"),
                    whatsAppConsentText = consentContainer.querySelector(".whatsAppConsentText"),
                    loginConsent = consentContainer.querySelector(".loginConsent"),
                    loginConsentText = consentContainer.querySelector(".loginConsentText");
                let validateOtpFormData = new FormData();
                //CONSENT Changes
                const formatComponent = (component) => String(component).padStart(2, '0');
                let consent = {
                    "product": {
                        "productType": "LOAN_APPLICATION",
                        "productTypeIdRef": null
                    },
                    "deviceTimeStamp": "",
                    "consentDataList": []
                };

                const now = new Date();
                consent.deviceTimeStamp = `${now.getFullYear()}-${formatComponent(now.getMonth() + 1)}-${formatComponent(now.getDate())} ${formatComponent(now.getHours())}:${formatComponent(now.getMinutes())}:${formatComponent(now.getSeconds())}`;
                if (pageCategory !== 'Credit Score Product Page' && pageCategory !== 'ProSaver Page' && pageCategory !== 'Credit Score Page' && pathname !== "/pro-saver-banking-faq" && pathname !== "/banking-faq") {
                    const addConsentObject = (type, text, value) => {
                        consent.consentDataList.push({
                            "productConsentType": type,
                            "consentText": text,
                            "consentValue": value.toUpperCase()
                        });
                    };
                    if (loginConsent) {
                        addConsentObject("LOGIN", loginConsentText.textContent, "GIVEN");
                    }
                    if (whatsAppConsent) {
                        addConsentObject("SIGN_UP_WHATSAPP", whatsAppConsentText.textContent, whatsAppConsentValue);
                    }
                    validateOtpFormData.append("consent", JSON.stringify(consent));
                }

                validateOtpFormData.append("otpId", otpId);
                validateOtpFormData.append("otp", otp);
                if (pageCategory === 'CT Campaign Page') validateOtpFormData.append("source", "ad-landing");
                else if (pageCategory === 'ProSaver Page' || pathname === "/pro-saver-banking-faq" || pathname === "/banking-faq") validateOtpFormData.append("source", "prosaver");

                request.send(validateOtpFormData);
            }
        }

        function resetDialog(parentContainer) {
            let paragraphElement = createDomElement("p", {
                "class": "bottom-sheet-btn-text-style ctButtonClass",
            }, 'Verify OTP');
            let parentElement = parentContainer.querySelector('.verify_otp_lead_gen_btn');
            parentContainer.querySelector('.verify_otp_lead_gen_btn').removeAttribute("disabled");
            while (parentElement.firstChild) parentElement.removeChild(parentElement.firstChild);
            parentElement.appendChild(paragraphElement);
            edit(document.querySelector(".lead_gen_form"));
        }

        function edit(parentContainer) {
            parentContainer.querySelectorAll(".verify_otp_input").forEach(element => {
                element.value = "";
            });
            parentContainer.querySelectorAll(".verify_otp_input").forEach(element => {
                element.classList.remove('error_border');
            });
            parentContainer.querySelector('.verify_otp_container').style.display = "none";
            parentContainer.querySelector('.otp_error_message').textContent = "";
            parentContainer.querySelector('.otp_error_message').classList.remove('display_block');
            parentContainer.querySelector('.generate_otp_container').style.display = "grid";
            parentContainer.querySelector(".consent_checkbox_wrap").style.display = "flex";
            parentContainer.querySelector('.edit_mobile_number_text').textContent = "";
            pageCategory === 'Credit Score Page' || pageCategory === 'CT Campaign Page' ? clearInterval(intervalId) : stopResendTime();
        }

        function handleOtpChange(event) {
            event.target.closest(".verify_otp_container").querySelector('.otp_error_message').classList.remove('display_block');
            const inputDivs = event.target.closest(".verify_otp_container").querySelectorAll(".verify_otp_input")
            inputDivs.forEach(element => {
                element.classList.remove('error_border');
            });
        }

        function setResendTime() {
            stopResendTime();
            timeOut = setTimeout(function () {
                document.querySelectorAll('.resend_otp').forEach(function (element) {
                    element.disabled = false;
                })
            }, 15000);
        }

        function stopResendTime() {
            document.querySelectorAll('.resend_otp').forEach(function (element) {
                element.disabled = true;
            })
            clearTimeout(timeOut);
        }

        setInterval(function () {
            const maxLength = 5;
            verifyOtpInput.forEach(function (element) {
                if (element.value > maxLength) element.value = element.value.slice(0, maxLength);
            });
        }, 100)

        function startTimer(duration, timerElement, resendButton) {
            clearInterval(intervalId);
            let seconds;
            intervalId = setInterval(function () {
                seconds = parseInt(duration % 60, 10);
                seconds = seconds < 10 ? "0" + seconds : seconds;
                timerElement.textContent = seconds + " " + "Sec";
                if (--duration < 0) {
                    clearInterval(intervalId);
                    document.querySelector('#otp-timer').style.display = "none";
                    $(".alert").hide();
                    resendButton.style.display = "inline-block";
                    resendButton.removeAttribute('disabled');
                    timerElement.innerHTML = '';
                }
            }, 1000);
        }

        function createDomElement(tagName, attributes = {}, ...items) {
            let element = document.createElement(tagName);

            for ([attributeKey, attributeValue] of Object.entries(attributes)) {
                if (attributeValue) {
                    element.setAttribute(attributeKey, attributeValue)
                }
            }

            if (items.length > 0) {
                let docFrag = document.createDocumentFragment();
                for (childEl of items) {
                    if (typeof childEl === "string") {
                        let div = document.createElement("div");
                        div.textContent = childEl;
                        while (div.childNodes.length > 0)
                            docFrag.appendChild(div.childNodes[0]);
                    } else if (childEl) {
                        docFrag.appendChild(childEl);
                    }
                }
                element.appendChild(docFrag);
            }
            return element;
        }

        if (!(pageCategory === 'Loan Campaign Page' || pageCategory === "CT Campaign Page") && isHidePopup !== "true") {
            function showPopup(popupType) {
                const isPopShownFlag = `${popupType}PopupShown`;
                let popupShown = sessionStorage.getItem(isPopShownFlag);
                if (!popupShown) {
                    $("#popupModal").modal("show");
                    sessionStorage.setItem(isPopShownFlag, "true");
                }
            }
            setTimeout(() => showPopup("websitepopup"), 5000);
        }
    });

    document.addEventListener("DOMContentLoaded", (event) => {
        let CTloginForm = document.querySelectorAll(".CTloginForm");
        let PLloginForm = document.querySelectorAll(".PLloginForm");
        let pageCategory = 'Brand Page';
        let [apiUrl, backendUrl, pwaUrl, creditUrl] = 'https://cms-api.moneyview.in/v1/api/,https://moneyview.whizdm.com/loans/services/api/web/,https://moneyview.in/apply-loan/,https://moneyview.in/credit-score/'.split(",");
        let redirectUrl = pageCategory === 'Credit Score Page' || pageCategory === 'CT Campaign Page' || pageCategory === 'Credit Score Product Page' ? creditUrl : pwaUrl;

        let continuePLApplication = document.querySelectorAll(".continuePLApplication");
        let continueCTApplication = document.querySelectorAll(".continueCTApplication");
        let pathname = '/about-us';
        let category = pageCategory.replace(/ /g, "_").toLowerCase() + "_leads";
        let pageUrl = pathname.split("/").pop();
        let utmSource, utmMedium, utmCampaign, queryString;

        if (pageCategory === 'Personal Loan Page') {
            utmSource = "personal_loan_page_leads";
            utmCampaign = "personal_loan_page_leads_continue_loan_application_cta";
            utmMedium = "pl_product_lps";
        } else if (pageCategory === 'Credit Score Product Page') {
            utmSource = "credit_tracker_page_leads";
            utmCampaign = "credit_tracker_page_leads_getscore_loggedinusers_cta";
            utmMedium = "ct_product_lps";
        } else {
            utmSource = category;
            utmMedium = "organic_channel";
            utmCampaign = `${category}_loggedin_users${pageUrl}`;
        }

        if (window.location.search) {
            queryString = new URLSearchParams(window.location.search.substring(1));
        } else {
            queryString = "utm_source=" + utmSource + "&utm_medium=" + utmMedium + "&utm_campaign=" + utmCampaign;
        }

        function isTokenExpired(token) {
            if (!token) {
                return true; // Token is missing
            }

            const tokenParts = token.split('.');
            if (tokenParts.length !== 3) {
                return true; // Invalid token format
            }

            const payload = JSON.parse(atob(tokenParts[1]));
            if (!payload || !payload.exp) {
                return true; // Token is missing expiration time
            }

            const expirationTime = payload.exp * 1000; // Convert expiration time from seconds to milliseconds
            const currentTime = Date.now(); // Get current time in milliseconds

            return expirationTime < currentTime; // Check if expiration time is less than current time
        }

        function toggleElements(elements, displayValue) {
            elements.forEach(element => element.style.display = displayValue);
        }

        function addAutoLoginEvent(buttonClass, redirectBaseUrl, queryString, token, prefixString) {
            document.querySelectorAll(buttonClass).forEach(button => {
                button.addEventListener("click", () => {
                    if (isTokenExpired(token)) {
                        removeTokenStrings(prefixString)
                        location.reload();
                    } else {
                        window.open(`${redirectBaseUrl}utility/autologin?${queryString}&autologin_token=${token}`, "_self")
                    }
                });
            });
        }

        function removeTokenStrings(prefixString) {
            // Create a shallow copy of localStorage
            const initialLocalStorage = Object.assign({}, localStorage);

            // Loop through the keys in the copied localStorage
            for (let i = 0; i < Object.keys(initialLocalStorage).length; i++) {
                const key = Object.keys(initialLocalStorage)[i];
                // Check if the key starts with the specified prefix
                if (key && key.startsWith(`${prefixString}`)) {
                    localStorage.removeItem(key); // Remove the item from localStorage
                }
            }
        }

        function handleToken(tokenKey, loginFormElement, continueApplicationElement, buttonClass, redirectBaseUrl) {
            let token = localStorage.getItem(tokenKey);
            let prefixString = tokenKey.substring(0, 3)

            toggleElements(loginFormElement, "grid");
            toggleElements(continueApplicationElement, "none");

            if (token) {
                if (isTokenExpired(token)) {
                    removeTokenStrings(prefixString)
                    toggleElements(loginFormElement, "grid");
                    toggleElements(continueApplicationElement, "none");
                } else {
                    toggleElements(loginFormElement, "none");
                    toggleElements(continueApplicationElement, "block");
                    addAutoLoginEvent(buttonClass, redirectBaseUrl, queryString, token, prefixString);
                }
            }
        }

        if (pageCategory !== 'ProSaver Page' && pathname !== "/pro-saver-banking-faq" && pathname !== "/banking-faq") {
            if (pageCategory === 'Credit Score Product Page' || pageCategory === 'Credit Score Page') {
                handleToken("ct_creditToken", CTloginForm, continueCTApplication, ".CTButton", redirectUrl);
            } else {
                handleToken("pl_pwaToken", PLloginForm, continuePLApplication, ".PLButton", redirectUrl);
            }
        }
    });

    window.addEventListener('pageshow', (event) => {
        // Preventing back-forward cache while navigating between PL Page and Loan Application Page
        if (localStorage.getItem("ct_creditToken") || localStorage.getItem("pl_pwaToken")) {
            if (event.persisted) {
                location.reload();
            }
        }
    });
</script> -->
<!-- <script>
    window.addEventListener('load', function (e) {

        function onCloseOfNavBar() {
            navbarsExampleDefault.style.display = "none";
            navbarsExampleDefault.style.height = '0px';
            overlayEffect.style.display = "none";
            setTimeout(function () {
                navbarsExampleDefault.classList.remove("show")
                navbarsExampleDefault.classList.remove("in")
            }, 100);
        };

        // navbar related scripts
        const navbarsExampleDefault = document.getElementById("navbarsExampleDefault");
        const overlayEffect = document.getElementById('overlayEffect');

        if (document.getElementById('openNavbar')) {
            document.getElementById('openNavbar').addEventListener("click", function () {
                navbarsExampleDefault.style.display = "flex";
                navbarsExampleDefault.style.height = '100%';
                overlayEffect.style.display = "block";
            });
        }

        if (document.getElementById('closeNavbar')) {
            document.getElementById('closeNavbar').addEventListener("click", function () {
                onCloseOfNavBar();
            });
        }

        // onclick of overlay nav menu should close
        overlayEffect.addEventListener("click", function (event) {
            onCloseOfNavBar();
        });

        //Sticky Footer
        let bottomSheet = document.querySelector('.bottom-sheet');

        function showBottomSheet() {
            window.addEventListener("scroll", function (e) {
                let hide = document.querySelector(".hideStickyFooterWithClass");
                if (hide && e.srcElement.scrollTop >= 700) {
                    bottomSheet.style.display = 'flex'
                } else {
                    if (bottomSheet.classList.contains("onScrollFooterFixed")) {
                        bottomSheet.style.display = 'flex'
                    } else {
                        bottomSheet.style.display = 'none'
                    }
                }
            }, true);
        }

        showBottomSheet();
    });
</script> -->
<!-- <script>
    window.addEventListener('load', function (e) {
        let [apiUrl] = 'https://cms-api.moneyview.in/v1/api/,https://moneyview.whizdm.com/loans/services/api/web/,https://moneyview.in/apply-loan/,https://moneyview.in/credit-score/'.split(",");

        // replace signup links in page content with the new signup link

        function createDomElement(tagName, attributes = {}, ...items) {
            let element = document.createElement(tagName);

            for ([attributeKey, attributeValue] of Object.entries(attributes)) {
                if (attributeValue) {
                    element.setAttribute(attributeKey, attributeValue)
                }
            }

            if (items.length > 0) {
                let docFrag = document.createDocumentFragment();
                for (childEl of items) {
                    if (typeof childEl === "string") {
                        let div = document.createElement("div");
                        div.textContent = childEl;
                        while (div.childNodes.length > 0)
                            docFrag.appendChild(div.childNodes[0]);
                    } else if (childEl) {
                        docFrag.appendChild(childEl);
                    }
                }
                element.appendChild(docFrag);
            }
            return element;
        }

        // disclaimer
        const pathname = '/about-us',
            listOfUrls = [
                "/personal-loan",
                "/personal-loan-interest-rates-and-charges",
                "/personal-loan-eligibility-check",
                "/personal-loan-documents",
                "/loans/top-up-personal-loan",
                "/loans/personal-loan-foreclosure-repayment-procedure",
                "/loans/instant-personal-loan-for-salaried-employee",
                "/loans/instant-personal-loan-for-self-employed",
                "/loans/instant-cash-loan-on-aadhar-card-and-pan-card",
                "/loans/apply-personal-loan-from-private-finance-company",
                "/loans/apply-instant-personal-loan-for-low-salary-income",
                "/loans/instant-personal-loan-for-students-in-india",
                "/loans/education-loan",
                "/loans/personal-loan-for-laptop",
                "/loans/travel-loan",
                "/loans/bike-loan",
                "/loans/apply-for-personal-loan-for-buying-car",
                "/loans/debt-consolidation-loan",
                "/loans/apply-for-medical-emergency-loan",
                "/loans/personal-loan-for-marriage",
                "/loans/apply-for-consumer-durable-loan-online",
                "/loans/apply-for-personal-loan-for-home-renovation-loan",
                "/loans/emergency-loan-online-in-india",
                "/loans/loan-for-credit-card-payments",
                "/loans/start-up-business-loans",
                "/loans/startup-business-loan-for-womens-in-india",
                "/loans/loan-schemes-for-women-in-india",
                "/loans/personal-loan-salary-less-than-20000",
                "/loans/personal-loan-salary-less-than-15000",
                "/loans/personal-loan-50000-rupees",
                "/loans/1-year-12-month-loans",
                "/loans/10000-rupees-instant-personal-loan",
                "/loans/5-lakh-personal-loan",
                "/loans/1-lakh-rupees-personal-loan",
                "/loans/appy-for-30000-rupees-personal-loan",
                "/loans/how-to-get-personal-loan-with-low-cibil-score",
                "/loans/how-much-personal-loan-can-you-get",
                "/loans/same-day-loans",
                "/loans/apply-for-mobile-phone-loan-online",
                "/loans/online-loan-app",
                "/loans/instant-cash-loan-in-1-hour-in-india",
                "/loans/instant-cash-loan-in-1-hour-in-mumbai",
                "/loans/instant-cash-loan-in-1-hour-in-chennai",
                "/loans/instant-cash-loan-in-1-hour-in-bangalore",
                "/loans/unsecured-bad-credit-loans",
                "/loans/loans-for-bad-credit-no-guarantor-no-broker",
                "/loans/personal-loan-for-self-employed-without-itr-and-no-income-proof",
                "/loans/instant-personal-loan-without-salary-slip",
                "/loans/instant-cash-loans-with-no-credit-checks-and-no-paperwork-in-india",
                "/loans/personal-loans-for-unemployed",
                "/loan-insights/how-to-apply-for-unemployment-personal-loan-with-no-job-no-income",
                "/loan-insights/personal-loan-for-pension-holder",
                "/loans/personal-loan-for-central-govt-employees",
                "/loans/loan-for-professional-courses-and-career-development",
                "/loans/apply-loan-for-professionals-doctors-engineers-chartered-accountant",
                "/loan-insights/education-loan-for-abroad-studies-without-security",
                "/loan-insights/types-of-personal-loans",
                "/loan-insights/tips-and-tricks-for-applying-personal-loan",
                "/loans/collateral-free-personal-loans",
                "/loans/personal-loan-without-documents",
                "/loans/loan-for-business-without-security",
                "/loan-insights/bank-loans-vs-private-lender-loans",
                "/loans/best-instant-personal-loan-apps-in-india",
                "/loan-insights/which-bank-is-best-for-personal-loan-in-india",
                "/loan-insights/pros-and-cons-of-unsecured-loans",
                "/loan-insights/different-types-of-loans-and-their-features",
                "/loans/advance-salary-loan-in-india",
                "/loans/instant-payday-loan-online",
                "/loans/nri-personal-loan-in-india",
                "/loan-insights/top-education-loan-providers-in-india",
                "/loan-insights/long-term-loan-details-in-india",
                "/loan-insights/short-term-loan-details-in-india",
                "/personal-loan/buy-mobile-phone-on-emi-without-credit-card",
                "/personal-loan/how-to-get-loan-for-used-bike-motorcycle",
                "/loan-insights/pre-approved-personal-loan-details",
                "/loan-insights/loan-facility-for-physically-handicapped-person",
                "/loans/details-on-loan-against-fixed-deposit",
                "/personal-loan-in-bangalore",
                "/personal-loan-in-chennai",
                "/personal-loan-in-pune",
                "/personal-loan-in-mumbai",
                "/personal-loan-in-delhi",
                "/personal-loan-in-hyderabad",
                "/personal-loan-in-kolkata",
                "/personal-loan-in-ahmedabad",
                "/personal-loan-in-kerela",
                "/personal-loan-in-surat",
                "/personal-loan-in-coimbatore",
                "/personal-loan-in-indore",
                "/personal-loan-in-patna",
                "/personal-loan-in-guwahati",
                "/personal-loan-in-tamilnadu",
                "/personal-loan-in-tirupur",
                "/personal-loan-in-chandigarh",
                "/personal-loan-in-kochi",
                "/personal-loan-in-dehradun",
                "/personal-loan-in-noida",
                "/personal-loan-in-kukatpally",
                "/loans/3-lakh-rupees-personal-loan",
                "/loans/apply-for-20000-rupees-personal-loan",
                "/hindi/personal-loan",
                "/hindi/loans/instant-personal-loan-on-aadhar-card-and-pan-card",
                "/loans/5000-rupees-personal-loan",
                "/insights/education-loan-for-mbbs",
                "/loans/best-nbfc-for-personal-loan",
                "/loans/apply-2-lakh-personal-loan",
                "/how-to-apply-for-personal-loan",
                "/hindi/loans/best-personal-loan-apps",
                "/hindi/emi-calculator/bike-loan-emi-calculator",
                "/hindi/loans/education-loan",
                "/hindi/loans/apply-for-medical-emergency-loan",
            ];

        if (listOfUrls.includes(pathname)) {
            const divCard =
                createDomElement("div", {
                        "id": "collapse-section",
                        "class": "accordion text-left container margin_accordion mt-3",
                    },
                    createDomElement("div", {
                            "class": "card",
                        },
                        createDomElement("div", {
                                "class": "card-header",
                                "id": "headingDisclaimer",
                            },
                            createDomElement("a", {
                                    "data-toggle": "collapse",
                                    "class": "collapsed",
                                    "data-target": "#collapseDisclaimer",
                                    "aria-expanded": "false",
                                    "aria-controls": "collapseDisclaimer",
                                },
                                "Disclaimer: ",
                                createDomElement("span", {
                                        "class": "pull-right",
                                    },
                                    createDomElement("i", {
                                        "class": "fa fa-angle-right",
                                    }),
                                ),
                            ),
                        ),
                        createDomElement("div", {
                                "id": "collapseDisclaimer",
                                "class": "collapse",
                                "aria-labelledby": "headingDisclaimer",
                                "data-parent": "#collapse-section",
                            },
                            createDomElement("div", {
                                    "class": "card-body",
                                },
                                createDomElement("p", {
                                        "class": "mv-why-us p20-top",
                                    },
                                    "*Starting interest rate varies on factors like credit history, obligation, or lender. #Indicative disbursement time measured for a loan of Rs 50,000 carried under controlled conditions. Subject to the necessary KYC and Income Verification. T&C apply.",
                                )
                            )
                        )
                    )
                )

            let parentDiv = document.querySelector('.footer').parentNode;
            parentDiv.insertBefore(divCard, document.querySelector('.footer'))
        }

        $('input').on('wheel', function (event) {
            event.preventDefault();
        });

        let feedbackForm = document.getElementById('feedbackForm');
        let feedbackComment = document.getElementById('feedbackComment');
        let submitFeedback = document.getElementById('submitFeedback');

        // feedback section
        if (feedbackForm) {
            const pageTitle = `About moneyview - Personal Loans and Money Management`,
                pageUrl = `/about-us`;
            let responseFromBE;

            const makeBECall = (choice) => {
                const url = `${apiUrl}feedback/submit`;

                let method, body, comment = feedbackComment.value;

                if (choice) {
                    document.getElementById('feedbackAction').style.display = 'none';
                    document.getElementById('feedbackActionSuccess').style.display = 'block';
                    document.getElementById('feedbackActionMessageMain').textContent = choice === 'yes' ? "We're glad to hear that!" : "We're sorry to hear that!";
                    document.getElementById('feedbackActionMessageSecondary').textContent = choice === 'yes' ? 'Your feedback will help us serve you better.' : "Please let us know how we can do better.";
                    method = 'POST';
                    body = {
                        choice,
                        title: pageTitle,
                        url: pageUrl,
                        comment
                    };
                } else {
                    method = "PUT";
                    body = {
                        choice: responseFromBE.choice,
                        title: responseFromBE.title,
                        url: responseFromBE.url,
                        mongoId: responseFromBE._id,
                        comment
                    };
                }

                let fetchData = {
                    method: method,
                    body: JSON.stringify(body),
                    headers: {
                        'Content-Type': 'application/json',
                        "x-access-apikey": "5df8e028-ed4c-4d37-9ee2-739183d7a565",
                    }
                }

                fetch(url, fetchData).then(res => {
                    return res.json();
                }).then(res => {
                    if (choice) {
                        responseFromBE = res;
                        document.getElementById('feedbackSection').style.display = 'block';
                    } else {
                        feedbackForm.style.display = "none";
                        document.getElementById("successSection").style.display = "block";
                    }
                })
            }

            let feedbackButtons = document.querySelectorAll('.feedbackHelpfulButton');
            feedbackButtons.forEach((eachButton) => {
                eachButton.addEventListener('click', (event) => {
                    event.preventDefault();
                    let choice = event.target.textContent.toLowerCase();
                    makeBECall(choice);
                });
            });

            feedbackComment.addEventListener('keyup', (event) => {
                submitFeedback.disabled = event.target.value.length > 0 ? false : true;
            });

            feedbackComment.addEventListener('keypress', (event) => {
                const validKeyCodes = [8, 32, 34, 38, 44, 46, 61, 48, 57, 64],
                    k = event.keyCode;
                if (!((k > 64 && k < 91) || (k > 96 && k < 123) || (k > 48 && k < 57) || validKeyCodes.includes(k))) {
                    event.preventDefault();
                }
            });

            submitFeedback.addEventListener('click', () => {
                if (feedbackComment.value.length > 0) makeBECall();
            });
        }

        $(".expandBtn").click(function () {
            if ($(this).hasClass("fa-angle-down")) {
                $(this).removeClass("fa-angle-down");
                $(this).addClass("fa-angle-up");
                $(".desktopToggleItems").each(function () {
                    $(this).removeClass("hideItem");
                });
            } else {
                $(this).addClass("fa-angle-down");
                $(this).removeClass("fa-angle-up");
                $(".desktopToggleItems").each(function () {
                    $(this).addClass("hideItem");
                });
            }
        });

        $(".features-btn").each(function () {
            $(this).on("click", function () {
                $(this).parent().parent().siblings().toggleClass("desktop");
                $(this).parent().parent().parent().toggleClass("valign-items-middle").toggleClass("paddingLeft28");
            });
        });

        function closeAccordion(accordion) {
            $(accordion).find("i").removeClass("fa-minus").addClass("fa-plus")
        }

        $(".accordionCard").on("click", function () {
            $(".accordionCard").each(function () {
                closeAccordion($(this))
            })
            if ($(this).find('a').attr("aria-expanded") === 'false') {
                let element = document.getElementById($(this)[0].id).previousSibling;
                if (element) {
                    element.scrollIntoView();
                } else {
                    let parentEl = document.getElementById($(this)[0].id).children[0]
                    parentEl.scrollIntoView();
                }
                $(this).find("i").removeClass("fa-plus").addClass("fa-minus")
            } else {
                closeAccordion($(this))
            }
        });

        $(".changeIcon").mouseover(function () {
            let interlinkImage = $(this).css("background-image");
            let hoverImage = interlinkImage.replace(/.png/g, "_hover.png");
            $(this).css("background-image", hoverImage);
        }).mouseout(function () {
            let hoverImage = $(this).css("background-image");
            let interlinkImage = hoverImage.replace(/_hover.png/g, ".png");
            $(this).css("background-image", interlinkImage);
        });

    });
</script> -->

<!-- <script type="text/javascript" id="">!function(b,e,f,g,a,c,d){b.fbq||(a=b.fbq=function(){a.callMethod?a.callMethod.apply(a,arguments):a.queue.push(arguments)},b._fbq||(b._fbq=a),a.push=a,a.loaded=!0,a.version="2.0",a.queue=[],c=e.createElement(f),c.async=!0,c.src=g,d=e.getElementsByTagName(f)[0],d.parentNode.insertBefore(c,d))}(window,document,"script","https://connect.facebook.net/en_US/fbevents.js");fbq("init","937243186330228");fbq("set","agent","tmgoogletagmanager","937243186330228");fbq("track","PageView");</script><script>
<
noscript > < img height = "1"
width = "1"
style = "display:none"
src = "https://www.facebook.com/tr?id=937243186330228&amp;ev=PageView&amp;noscript=1" > < /noscript>

    ! function (b, e, f, g, a, c, d) {
        b.fbq || (a = b.fbq = function () {
            a.callMethod ? a.callMethod.apply(a, arguments) : a.queue.push(arguments)
        }, b._fbq || (b._fbq = a), a.push = a, a.loaded = !0, a.version = "2.0", a.queue = [], c = e.createElement(f), c.async = !0, c.src = g, d = e.getElementsByTagName(f)[0], d.parentNode.insertBefore(c, d))
    }(window, document, "script", "https://connect.facebook.net/en_US/fbevents.js");
fbq("init", "1094686184251880");
fbq("set", "agent", "tmgoogletagmanager", "1094686184251880");
fbq("track", "PageView");
</script><script>
<
noscript > < img height = "1"
width = "1"
style = "display:none"
src = "https://www.facebook.com/tr?id=1094686184251880&amp;ev=PageView&amp;noscript=1" > < /noscript>
    (function (a, e, b, f, g, c, d) {
        a[b] = a[b] || function () {
            (a[b].q = a[b].q || []).push(arguments)
        };
        c = e.createElement(f);
        c.async = 1;
        c.src = "https://www.clarity.ms/tag/" + g;
        d = e.getElementsByTagName(f)[0];
        d.parentNode.insertBefore(c, d)
    })(window, document, "clarity", "script", "91d9ud7w3l");
</script> -->
</template>


<style>
    .paddingInline0 {
        padding-left: 0px;
        padding-right: 0px;
    }

    .marginInline0 {
        margin-left: 0px;
        margin-right: 0px;
    }

    @media screen and (min-width: 360px) {
        .our-mission {
            width: 100%;
            height: 180px;
            margin-top: 32px;
            background-image: url('https://moneyview.in/images/home_banner_mobile.png');
            background-color: #453456;
            background-position: right;
            background-repeat: no-repeat;
            padding: 24px 24px 48px 24px;
        }

        .our-mission .heading {
            width: 148px;
            margin-bottom: 16px !important;
            font-weight: 350;
            font-size: 26px;
            line-height: 32px;
            color: #FFFFFF;
        }

        .our-mission .content {
            width: 224px;
            height: 88px;

            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #FFFFFF;
            opacity: 0.4;
        }
    }

    @media screen and (min-width: 480px) and (max-width: 768px) {
        .our-mission .content {
            height: 60px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 960px) {
        .our-mission {
            padding: 24px 32px 48px 32px;
        }

        .our-mission .content {
            height: 60px;
        }
    }

    @media screen and (min-width: 960px) and (max-width: 1440px) {
        .our-mission {
            padding: 24px 40px 48px 40px;
        }

        .our-mission .content {
            height: 60px;
        }
    }

    @media screen and (min-width: 1440px) and (max-width: 1920px) {
        .our-mission {
            width: 100%;
            height: 352px;
            margin-top: 62px;
            background-image: url('https://moneyview.in/images/header_banner.png');
            background-color: #453456;
            padding: 100px 0px 80px 100px;
        }

        .our-mission .heading {
            width: 273px;
            height: 56px;
            font-weight: 350;
            font-size: 48px;
            line-height: 56px;
            color: #FFFFFF;
            margin-bottom: 24px !important;
        }

        .our-mission .content {
            width: 596px;
            height: 72px;
            font-weight: 350;
            font-size: 24px;
            line-height: 36px;
            color: #FFFFFF;
            opacity: 0.4;
        }
    }

    @media screen and (min-width: 1920px) {
        .our-mission {
            width: 100%;
            height: 352px;
            margin-top: 62px;
            background-image: url('https://moneyview.in/images/header_banner.png');
            background-color: #453456;
            padding: 82px 0px 82px 120px;
        }

        .our-mission .heading {
            width: 319px;
            height: 68px;
            font-weight: 350;
            font-size: 56px;
            line-height: 68px;
            color: #FFFFFF;
            margin-bottom: 32px !important;
        }

        .our-mission .content {
            width: 795px;
            height: 88px;
            font-weight: 350;
            font-size: 32px;
            line-height: 44px;
            color: #FFFFFF;
            opacity: 0.4;
        }
    }

    @media screen and (min-width: 360px) {
        .section-2 {
            width: 100%;
            padding: 24px;
            display: flex;
            flex-direction: column-reverse;
        }

        .section-2 .key-principles {
            width: 100%;
        }

        .section-2 .key-principles .heading {
            width: 195px;
            font-weight: 350;
            font-size: 26px;
            line-height: 32px;
        }

        .section-2 .key-principles .content {
            display: flex;
            flex-direction: column;
            margin-top: 24px;
        }

        .section-2 .key-principles .content .principle {
            display: flex;
        }

        .section-2 .key-principles .content .principle img {
            height: 64px;
            width: 64px;
            margin-right: 24px;
        }

        .section-2 .key-principles .content .principle h2 {
            font-weight: 350;
            font-size: 18px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 8px;
        }

        .section-2 .key-principles .content .principle p {
            width: 93%;
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
            padding-bottom: 0px;
            margin-bottom: 36px;
        }

        .section-2 .key-principles .content .principle:nth-child(1) h2 {
            width: 158px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) h2 {
            width: 225px;
        }

        .section-2 .key-principles .content .principle:nth-child(3) h2 {
            width: 225px;
        }

        .section-2 .key-principles .content .principle:nth-child(3) p {
            margin-bottom: 0px;
        }

        .section-2 .content {
            width: 100%;
            margin-top: 24px;
        }

        .section-2 .content p {
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
            margin-bottom: 36px;
        }

        .sectionHeading {
            font-weight: 600;
            font-size: 18px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 8px;
        }

        .sectionContent {
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
            padding-bottom: 0px;
            margin-bottom: 36px;
        }
    }

    @media screen and (min-width: 480px) and (max-width: 768px) {
        .section-2 .key-principles .content .principle p {
            max-width: 344px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) h2 {
            width: 158px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 900px) {
        .section-2 {
            padding: 32px;
        }

        .section-2 .key-principles .content .principle p {
            max-width: 364px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) h2 {
            width: 158px;
        }
    }

    @media screen and (min-width: 960px) and (max-width: 1440px) {
        .section-2 {
            padding: 40px;
        }

        .section-2 .key-principles .content .principle p {
            max-width: 364px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) h2 {
            width: 158px;
        }
    }

    @media screen and (min-width: 1440px) and (max-width: 1920px) {
        .section-2 {
            width: 100%;
            padding: 120px 80px 0px;
            display: flex;
            flex-direction: row;
            justify-content: space-between;
        }

        .section-2 .key-principles {
            width: calc(60vw - 96px);
        }

        .section-2 .key-principles .heading {
            width: 360px;
            font-weight: 350;
            font-size: 48px;
            line-height: 56px;
            color: #000000;
        }

        .section-2 .key-principles .content {
            display: flex;
            flex-direction: row;
            margin-top: 64px;
        }

        .section-2 .key-principles .content .principle {
            display: block;
        }

        .section-2 .key-principles .content .principle img {
            height: 72px;
            width: 72px;
            margin-bottom: 24px;
        }

        .section-2 .key-principles .content .principle:nth-child(1) h2 {
            width: 113px;
            font-weight: 600;
            font-size: 18px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 8px;
        }

        .section-2 .key-principles .content .principle:nth-child(1) p {
            width: 15vw;
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
        }

        .section-2 .key-principles .content .principle:nth-child(2) {
            padding-inline: 32px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) h2 {
            width: 193px;
            font-weight: 600;
            font-size: 18px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 8px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) p {
            width: 15vw;
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
        }

        .section-2 .key-principles .content .principle:nth-child(3) h2 {
            width: 135px;
            font-weight: 600;
            font-size: 18px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 8px;
        }

        .section-2 .key-principles .content .principle:nth-child(3) p {
            width: 17vw;
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
        }

        .section-2 .content {
            width: calc(40vw - 96px);
        }

        .section-2 .content p {
            font-weight: 350;
            font-size: 16px;
            line-height: 24px;
            color: #565378;
        }
    }

    @media screen and (min-width: 1920px) {
        .section-2 {
            width: 100%;
            padding: 120px 120px 0px;
            display: flex;
            flex-direction: row;
            justify-content: space-between;
        }

        .section-2 .key-principles {
            width: calc(60vw - 96px);
        }

        .section-2 .key-principles .heading {
            width: 420px;
            font-weight: 350;
            font-size: 56px;
            line-height: 68px;
            color: #000000;
        }

        .section-2 .key-principles .content {
            display: flex;
            flex-direction: row;
            margin-top: 32px;
        }

        .section-2 .key-principles .content .principle {
            display: block;
        }

        .section-2 .key-principles .content .principle img {
            height: 94px;
            width: 94px;
            margin-bottom: 24px;
        }

        .section-2 .key-principles .content .principle:nth-child(1) h2 {
            width: 138px;
            font-weight: 600;
            font-size: 22px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 12px;
        }

        .section-2 .key-principles .content .principle:nth-child(1) p {
            width: 12vw;
            font-weight: 350;
            font-size: 16px;
            line-height: 24px;
            color: #565378;
        }

        .section-2 .key-principles .content .principle:nth-child(2) {
            padding-inline: 76px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) h2 {
            width: 193px;
            font-weight: 600;
            font-size: 22px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 12px;
        }

        .section-2 .key-principles .content .principle:nth-child(2) p {
            width: 11vw;
            font-weight: 350;
            font-size: 16px;
            line-height: 24px;
            color: #565378;
        }

        .section-2 .key-principles .content .principle:nth-child(3) h2 {
            width: 135px;
            font-weight: 600;
            font-size: 22px;
            line-height: 24px;
            color: #000000;
            padding-bottom: 0px;
            margin-bottom: 12px;
        }

        .section-2 .key-principles .content .principle:nth-child(3) p {
            width: 19vw;
            font-weight: 350;
            font-size: 16px;
            line-height: 24px;
            color: #565378;
        }

        .section-2 .content {
            width: calc(40vw - 96px);
            margin-top: 35px;
        }

        .section-2 .content p {
            font-weight: 350;
            font-size: 22px;
            line-height: 32px;
            color: #565378;
        }
    }

    .about-us-section-image {
        padding: 0px;
        flex: 0;
    }

    @media screen and (min-width: 360px) {
        .about-us-section {
            background-image: url(https://moneyview.in/images/about_us_mobile.png);
            background-repeat: no-repeat;
            background-position: right 0% top 120px;
            padding: 0px 24px 48px;
            background-size: 216px;
        }

        .about-us-section-title {
            font-weight: 600;
            font-size: 18px;
            line-height: 24px;
            color: #000000;
            margin-bottom: 4px;
        }

        .about-us-section-content {
            width: 74px;
            height: 40px;
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #434061;
            margin-bottom: 36px;
        }

        .about-us-section-paragraph {
            font-weight: 350;
            font-size: 20px;
            line-height: 26px;
            color: #000000;
            padding-block: 24px 36px;
            margin-bottom: 0px;
        }

        .about-us-section-image-container {
            width: 24px;
            height: 24px;
        }

        .about-us-section-container {
            padding-left: 12px;
        }
    }

    @media screen and (min-width: 480px) and (max-width: 768px) {
        .about-us-section-paragraph {
            padding-block: 12px 36px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 960px) {
        .about-us-section {
            background-image: url(https://moneyview.in/images/mobile_images_tablet.png);
            background-repeat: no-repeat;
            background-position: right 14% top 20px;
            padding: 0px 32px 60px;
            background-size: 300px 410px;
        }

        .about-us-section-paragraph {
            padding-block: 20px 36px;
        }
    }

    @media screen and (min-width: 960px) and (max-width: 1440px) {
        .about-us-section {
            background-image: url(https://moneyview.in/images/mobile_images_tablet.png);
            background-repeat: no-repeat;
            background-position: right 19% top 20px;
            padding: 0px 40px 60px;
            background-size: 300px 410px;
        }
    }

    @media screen and (min-width: 1440px) and (max-width: 1920px) {
        .about-us-section-display {
            display: flex;
        }

        .about-us-section {
            background-image: url(https://moneyview.in/images/about_us_desktop.png);
            background-repeat: no-repeat;
            background-position: right 0% top 80px;
            padding: 0px 80px 110px;
            background-size: 621px 443px;
        }

        .about-us-section-title {
            font-weight: 600;
            font-size: 30px;
            line-height: 36px;
            color: #000000;
            margin-bottom: 8px;
        }

        .about-us-section-content {
            width: 96px;
            height: 56px;
            font-weight: 350;
            font-size: 20px;
            line-height: 28px;
            color: #434061;
            margin-bottom: 0px;
        }

        .about-us-section-paragraph {
            font-weight: 350;
            font-size: 28px;
            line-height: 36px;
            color: #000000;
            padding-block: 120px 80px;
            margin-bottom: 0px;
        }

        .about-us-section-image-container {
            width: 32px;
            height: 32px;
        }

        .about-us-section-container {
            padding: 0px 0px 0px 8px;
        }

        .section-padding {
            padding-right: 80px;
        }
    }

    @media screen and (min-width: 1920px) {
        .about-us-section-display {
            display: flex;
        }

        .about-us-section {
            height: 628px;
            background-image: url(https://moneyview.in/images/about_us_desktop.png);
            background-repeat: no-repeat;
            background-position: right 0% top 80px;
            padding: 0px 120px 168px;
            background-size: 865px 498px;
        }

        .about-us-section-title {
            font-weight: 600;
            font-size: 36px;
            line-height: 36px;
            color: #000000;
            margin-bottom: 12px;
        }

        .about-us-section-content {
            width: 96px;
            height: 56px;
            font-weight: 350;
            font-size: 24px;
            line-height: 32px;
            color: #434061;
            margin-bottom: 0px;
        }

        .about-us-section-paragraph {
            font-weight: 350;
            font-size: 36px;
            line-height: 48px;
            color: #000000;
            padding-block: 120px 80px;
            margin-bottom: 0px;
        }

        .about-us-section-image-container {
            width: 40px;
            height: 40px;
        }

        .about-us-section-container {
            padding: 0px 0px 0px 12px;
        }

        .section-padding {
            padding-right: 85px;
        }
    }

    .margin0 {
        margin: 0px !important;
    }

    .display-flex {
        display: flex;
    }

    .investor-class {
        display: flex;
        flex-flow: wrap;
    }

    @media screen and (min-width: 360px) {
        .section-heading {
            font-weight: 350;
            font-size: 26px;
            line-height: 32px;
            color: #000000;
            margin-bottom: 24px;
        }

        .investor-section {
            background: #E4F1EA;
            padding: 48px 24px 8px 24px;
        }

        .investors-container {
            width: 138px;
            height: 50px;
            padding: 0px;
            flex: 0;
        }

        .investors-logo {
            width: 138px;
            height: 50px;
        }

        .investor-class div:nth-child(odd) {
            margin: 0px 16px 16px 0px;
        }

        .investor-class div:nth-child(even) {
            margin: 0px 16px 16px 0px;
        }
    }

    @media screen and (min-width: 480px) and (max-width: 768px) {
        .investor-class div:nth-child(odd) {
            margin: 0px 24px 16px 0px;
        }

        .investor-class div:nth-child(even) {
            margin: 0px 24px 16px 0px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 960px) {
        .investor-class div:nth-child(odd) {
            margin: 0px 60px 16px 0px;
        }

        .investor-class div:nth-child(even) {
            margin: 0px 60px 16px 0px;
        }
    }

    @media screen and (min-width: 960px) and (max-width: 1440px) {
        .investor-section {
            height: 158px;
            background: #E4F1EA;
            padding: 20px 40px 32px;
            margin: 0px;
        }

        .investors-container {
            margin: 0px 40px 0px 0px !important;
            padding: 0px;
            flex: 0;
        }

        .investor-class> :last-child {
            margin: 0px !important
        }
    }

    @media screen and (min-width: 1440px) and (max-width: 1920px) {
        .investor-section {
            height: 196px;
            background: #E4F1EA;
            padding: 20px 80px 52px;
            margin: 0px;
        }

        .investors-container {
            width: 176px;
            height: 64px;
            margin: 0px 100px 0px 0px !important;
            padding: 0px;
            flex: 0;
        }

        .investors-logo {
            width: 176px;
            height: 64px;
        }

        .investor-class div:nth-child(odd) {
            margin: 0px;
        }

        .investor-class div:nth-child(even) {
            margin: 0px;
        }

        .investor-class> :last-child {
            margin: 0px !important;
        }
    }

    @media screen and (min-width: 1920px) {
        .section-heading {
            font-weight: 350;
            font-size: 56px;
            line-height: 68px;
            color: #000000;
            margin-bottom: 32px;
        }

        .investor-section {
            height: 355px;
            background: #E4F1EA;
            padding: 60px 120px 96px;
            margin: 0px;
        }

        .investors-container {
            width: 276px;
            height: 100px;
            margin: 0px 74px 0px 0px !important;
            padding: 0px;
            flex: 0;
        }

        .investors-logo {
            width: 276px;
            height: 100px;
        }

        .investor-class> :last-child {
            margin: 0px !important;
        }
    }

    .founders-details {
        display: flex;
    }

    @media screen and (min-width: 360px) {
        .founders-section {
            padding: 48px 24px 20px;
        }

        .founders-border-box {
            width: 98%;
            background: #FFFFFF;
            border: 1px solid #ACABBD;
            border-radius: 16px;
            box-shadow: 8px 8px #b6e7ec;
            padding: 20px;
            margin: 0px 0px 28px 0px;
        }

        .box-shadow-color {
            box-shadow: 8px 8px #F5E4FF !important;
        }

        .founders-image {
            width: 64px;
            height: 64px;
        }

        .founders-class {
            margin: auto 0px;
            padding-left: 16px;
        }

        .founders-name {
            font-weight: 350;
            font-size: 18px;
            line-height: 20px;
            color: #000000;
            margin-bottom: 4px;
        }

        .founders-title {
            font-weight: 350;
            font-size: 12px;
            line-height: 20px;
            color: #565378;
            margin-bottom: 0px;
        }

        .founders-content {
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
            margin: 20px 0px 0px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 960px) {
        .founders-section {
            padding: 40px 32px 22px;
        }

        .founders-border-box {
            width: 99%;
        }
    }

    @media screen and (min-width: 960px) and (max-width: 1440px) {
        .founders-section-display {
            display: flex
        }

        .founders-section-display>div:nth-child(1) {
            margin: 0px 24px 20px 0px;
        }

        .founders-section {
            padding: 40px 40px 10px;
        }

        .founders-border-box {
            width: 98%px;
            height: 304px;
        }
    }

    @media screen and (min-width: 1440px) and (max-width: 1920px) {
        .founders-section-display {
            display: flex
        }

        .founders-section-display>div:nth-child(1) {
            margin: 0px 40px 20px 0px;
        }

        .founders-section {
            padding: 80px 80px 60px;
        }

        .founders-border-box {
            width: 98%;
            height: 244px;
            background: #FFFFFF;
            border: 1px solid #ACABBD;
            border-radius: 16px;
            box-shadow: 8px 8px #b6e7ec;
            padding: 20px 20px 20px 20px;
            margin: 0px 0px 20px 0px;
        }

        .box-shadow-color {
            box-shadow: 8px 10px #F5E4FF !important;
        }

        .founders-image {
            width: 64px;
            height: 64px;
        }

        .founders-name {
            font-weight: 350;
            font-size: 18px;
            line-height: 20px;
            color: #000000;
            margin-bottom: 4px;
        }

        .founders-class {
            margin: auto 0px;
            padding-left: 16px;
        }

        .founders-title {
            font-weight: 350;
            font-size: 12px;
            line-height: 20px;
            color: #565378;
            margin-bottom: 0px;
        }

        .founders-content {
            font-weight: 350;
            font-size: 14px;
            line-height: 20px;
            color: #565378;
            margin: 20px 0px 0px;
        }
    }

    @media screen and (min-width: 1920px) {
        .founders-section-display {
            display: flex
        }

        .founders-section-display>div:nth-child(1) {
            margin: 0px 24px 20px 0px;
        }

        .founders-section {
            padding: 120px 120px 60px;
        }

        .founders-border-box {
            width: 828px;
            height: 424px;
            background: #FFFFFF;
            border: 1px solid #ACABBD;
            border-radius: 20px;
            box-shadow: 10px 10px #b6e7ec;
            padding: 40px 46px 40px 40px;
            margin: 0px 0px 20px 0px;
        }

        .founders-image {
            width: 96px;
            height: 96px;
        }

        .founders-name {
            font-weight: 350;
            font-size: 24px;
            line-height: 36px;
            color: #000000;
            margin-bottom: 4px;
        }

        .founders-class {
            margin: auto 0px;
            padding-left: 24px;
        }

        .founders-title {
            font-weight: 350;
            font-size: 16px;
            line-height: 24px;
            color: #565378;
            margin-bottom: 0px;
        }

        .founders-content {
            font-weight: 350;
            font-size: 22px;
            line-height: 32px;
            color: #565378;
            margin: 24px 0px 0px;
        }
    }
</style>